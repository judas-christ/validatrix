//>>excludeStart("exclude", pragmas.exclude);
//>>group: exclude
require([
    'validatrix.js',
    // 'events/modern.js', //for modern browsers only, or if you're using a polyfill for addEventListener
    'events/oldies.js', //for IE8 compatibility
    'validators/required.js',
    'validators/regex.js',
    'validators/length.js',
    'validators/number.js',
    'validators/range.js',
    // 'events/defaultValidationEvents.js' //very basic invalid/valid handlers, mimicking the default unobtrusive validation behaviour
]);
//>>excludeEnd("exclude");
